import "package:flutter/material.dart";
class AchievementsScreen extends StatelessWidget {
  const AchievementsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("업적")), body: const Center(child: Text("업적 목록")));
}
