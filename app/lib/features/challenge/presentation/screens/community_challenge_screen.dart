import "package:flutter/material.dart";
class CommunityChallengeScreen extends StatelessWidget {
  const CommunityChallengeScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("커뮤니티 챌린지")), body: const Center(child: Text("커뮤니티 챌린지 상세")));
}
