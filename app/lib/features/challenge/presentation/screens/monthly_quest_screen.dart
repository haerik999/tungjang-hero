import "package:flutter/material.dart";
class MonthlyQuestScreen extends StatelessWidget {
  const MonthlyQuestScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("월간 퀘스트")), body: const Center(child: Text("월간 퀘스트 상세")));
}
