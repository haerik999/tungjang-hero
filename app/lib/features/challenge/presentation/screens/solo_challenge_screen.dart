import "package:flutter/material.dart";
class SoloChallengeScreen extends StatelessWidget {
  const SoloChallengeScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("솔로 챌린지")), body: const Center(child: Text("솔로 챌린지 상세")));
}
