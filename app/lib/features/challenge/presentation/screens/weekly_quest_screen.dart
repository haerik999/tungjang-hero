import "package:flutter/material.dart";
class WeeklyQuestScreen extends StatelessWidget {
  const WeeklyQuestScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("주간 퀘스트")), body: const Center(child: Text("주간 퀘스트 상세")));
}
